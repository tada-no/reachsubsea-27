Let's build the **Assets overview** page (`/assets/`, nav label "Fleet overview") in the reach-web-2027 prototype. It is the landing page of the top-level **Assets** section: Vessels, Reach Remote (with Reach Remote 3 & 4 under it), ROVs and Survey & monitoring equipment sit under it as siblings. The single asset pages and the sibling listings are **not** part of this task; link to them.

**Read first**
- `CLAUDE.md` and `docs/08-page-review-checklist.md`. The review checklist is mandatory: run it yourself, at 1440 · ~1100 · 800 · 375, before showing me anything. §7 is a token audit (grep commands): run it before you hand over.
- Your memory for this project. The ones that bite on a fleet page: creative not generic, hub pages keep the key content of their children, card rows share one structure, visual flow with no fragmented text, hero image choice, data viz restraint, type and tablet layouts, service page lessons, photos approved as a rule.
- `docs/05-blocks-spec.md` (§0 rules, §2 blocks, §3 page layouts: the "Asset single" and "Reach Remote" rows sketch the children) and `src/blocks/README.md`. There are 22 blocks in code; the Card grid, Card bento, Figures, Split media, Split embed, Stats band and Results band are separate blocks now (Q67), so pick the right one.
- `docs/00-questions.md`: Q57 (photo or video heroes on story pages, text heroes on data pages), Q63 (Subsea), Q66 (Careers), Q67, Q69. Use **Q70** for your first decision. Q69 is the token rule: no raw font sizes, colours, spacings, leading or letter-spacing in blocks. Snap to a token; only add one if a repeated role has none (then add its Figma variable too).
- Closest siblings to reuse rather than copy: `/services/` and `/services/subsea/` (Card bento patterns, Lifecycle, pictograms, project cards from `src/data/projects.ts`), `/careers/` (hero, Split media Wide, Stats band Feature) and `/investors/` (Timeline Track, Card bento lead-quad). `scripts/shot.mjs` takes screenshots at any width and scroll position.
- `git status` and `git log` first. Another chat may be working in this repo: stage only your own files, by explicit path.

**Source content**
- Client PDF `ref/Reach Subsea Website - Full Resolution Page Screens.pdf`, page 10 ("Assets & Fleet", a 5-screen page: about 9 vessel cards, 4 ROV cards, newbuilds and DriX). Pages are images: render with pymupdf (`fitz`) into the scratchpad. Strip internal editorial notes. `docs/03-client-pdf-review.md` §C rates it "Dev": the dev site's Asset post type is the better solution, so use the PDF for intent and the dev site for the data. The PDF may show a stats panel overlapping the hero: **never do that, site-wide.** Put it in the first block below the hero.
- Dev/live sites: https://reachsubsea.no/assets/ and https://reachsubseadev.wpenginepowered.com/assets/ (plain `curl` works). About 20 assets: vessels (Olympic Taurus, Olympic Triton, Deep Cygnus, Havila Subsea, Northern Maria, Go Electra, Viking Reach 2, Viking Vigor, Offshore Surveyor, Surveyor Interceptor), Reach Remote 1–4 (RR 3 & 4 are newbuilds), DriX Orca 1 and 2, ROVs (Supporter WROV, Constructor WROV, ZeeROV, Schilling HD WROV) and a "newbuild" entry. Dev lists them under `/company/assets/vessels|remote-vessels|rovs/`; the sitemap on the live site uses `/assets/<slug>/`. Read `ref/reach sitemap from client.jpg`, list which old URLs should redirect to `/assets/…`, and say which content wins.
- Do not invent specs, dimensions or dates. Take every number from the dev asset pages or the PDF and put it in a new `src/data/assets.ts` with a source per entry, plus a WP proposal in the header comment (Asset post type: `type` vessel / remote vessel / ROV / equipment, `status` in service / newbuild / sold, spec fields, related projects and services), the way `src/data/projects.ts` does it. Fleet counts on the page (vessels, ROVs, Reach Remote units) must be derived from that data or from `src/data/key-figures.ts`, never hand-typed twice. Vessels sold or leaving the fleet (for example Viking Reach) need a status; ask me if the status of an asset is unclear.
- Photos already in `public/images/`: `asset-constructor-wrov`, `asset-havila-subsea`, `asset-supporter-wrov`, `vessel-agalas`, `vessel-olympic-zeus`, `reach-remote-bow`, `reach-remote-side`, `usv-drix-wind`, `rov-supporter-launch`, `rov-supporter-subsea`, `rov-surveyor-launch`, `rov-zeerov-gwatch`, `rov-zeerov-seabed`, `rov-zeerov-wellhead`, `ocean-horizon-calm`, `3d-world-poster`. Several are still unused. Photos from the live and dev sites (people too) need no per-photo permission; the site-wide tint applies automatically. Fetch more from the dev asset pages if a vessel has none. Check `feedback-hero-image-choice` before picking the hero and tell me if a better image is needed from the client.
- Pictograms: `seabed-repair` and `survey-rov` exist but are unused; `Pictogram` takes any file in `src/assets/pictograms`. New ones must match the loop feel of the others (trim paths, movement, rotation, scale; solid colour only, no transparency; 3.2s cycle, rests at 0% and 100%), and if one exists on the live/dev site, extract the original SVG instead of redrawing it.

**Ideas, not requirements**
- The fleet is the one place on the site where scale is the story. Consider a slim, custom "fleet at a glance" (counts by type and status, in service vs newbuild, crewed vs uncrewed) driven by `assets.ts`, in the spirit of the investor-year track: short labels, quiet ticks, markers highlight.
- Group by what a visitor is deciding: a client looking for a vessel that can do a job, or an engineer looking for an ROV class. Feed grid with facets (type, status) is the WordPress-friendly answer; think about what the first screen below the hero should show before the filters.
- Reach Remote deserves more than one card: it is the uncrewed story and it links to the 3D World. Reuse the 3D World embed or a poster link only if it earns its place.
- Be far more creative than the PDF's layout, within the design system.

**Tie it together**
- Hero: breadcrumb Home › Assets, same pattern as Careers and Subsea. Section subnav from the Assets panel in `src/data/navigation.ts` (Vessels · Reach Remote · ROVs · Survey & monitoring equipment; add "Overview" first if that matches the other sections), Overview active. Link the siblings even though those pages aren't built.
- CTA: short header, one-line body, the PDF's wording. No highlighted CTA buttons in the nav.
- Fleet-wide figures only from `src/data/key-figures.ts` or `assets.ts`.

**How to work**
1. Look at PDF page 10, both sites and our Services/Subsea pages; propose the page outline with a one-line reason per section, and what you would cut or add from the PDF, by visitor intent. Ask me anything open with AskUserQuestion (recommended option first) and log the answers in `docs/00-questions.md` as you go.
2. Build section by section. After each section run the checklist at all four widths: card rows share one structure with their actions lined up, no hairlines inside cards, one-line meta, no half-empty cards or tablet rows, no tint-on-tint, figures fit, text columns flow, grounds alternate (use a navy block to break up any text-heavy stretch), contrast holds. Fix what fails before showing me. Headless Chrome via `scripts/shot.mjs` can do 375; the in-app browser pane can't screenshot while hidden.
3. Show me screenshots with a line on what you checked and fixed.
4. After I approve:
   - Update `docs/05` §3 (replace or add the Assets overview row) and `docs/00-questions.md`.
   - Update Figma (file `HAvCQCXzWNFOKQ1AZxqNTX`): new tokens, components and block variants, plus an "Assets overview" frame on the Pages page (`250:22`, to the right of the last frame). Record the IDs in `docs/extract/figma-blocks-ledger.json`. Load the `figma-use` skill first and make Figma writes one agent at a time. Photos can't be placed by the API: use placeholders and list them.
   - Commit only your own files by path; if a shared file also holds another chat's hunks, stage only yours and check the staged snapshot builds on its own (`git checkout-index -a --prefix=<tmp>/`, symlink `node_modules`, `npx astro build --outDir <tmp>`). Ask me before you push.

**Practicalities**
- `npm run dev` serves on :4321, or set `PORT=<n>` to run your own. If another chat owns :4321, browse it instead (same working tree). Never use `--force`. If styles look stale, `touch` the changed files.
- Use cheaper subagents (Sonnet or Haiku) for mechanical sub-tasks (site survey, PDF rendering, the asset data extraction) and tell me if it is worth starting a new chat partway through.
